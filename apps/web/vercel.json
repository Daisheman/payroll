{
  "buildCommand": "cd ../.. && npm install --no-audit --no-fund && npm --workspace packages/schemas run build && npm --workspace apps/web run build",
  "outputDirectory": ".next",
  "installCommand": "cd ../.. && npm install --no-audit --no-fund",
  "framework": "nextjs"
}
